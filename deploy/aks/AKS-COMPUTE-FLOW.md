# USF BIOS - AKS Deployment Compute Flow

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              AZURE KUBERNETES SERVICE (AKS)                          │
│                                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────────────┐   │
│  │                            AKS Node Pool (GPU)                               │   │
│  │                         (e.g., Standard_NC6s_v3)                             │   │
│  │                                                                              │   │
│  │   ┌──────────────────────────────────────────────────────────────────────┐  │   │
│  │   │                         USF BIOS Pod                                  │  │   │
│  │   │                                                                       │  │   │
│  │   │   ┌─────────────┐         ┌─────────────────────┐                    │  │   │
│  │   │   │   Next.js   │ ──────► │   FastAPI Backend   │                    │  │   │
│  │   │   │  Frontend   │  proxy  │   (127.0.0.1:8000)  │                    │  │   │
│  │   │   │  Port 3000  │         │   INTERNAL ONLY     │                    │  │   │
│  │   │   └─────────────┘         └──────────┬──────────┘                    │  │   │
│  │   │         ▲                            │                               │  │   │
│  │   │         │                            ▼                               │  │   │
│  │   │   ┌─────┴─────────────────────────────────────────────────────────┐  │  │   │
│  │   │   │                    Volume Mounts                              │  │  │   │
│  │   │   │   /app/data ────────────────────────────────────────────────► │──┼──┼───┤
│  │   │   │   /models   ────────────────────────────────────────────────► │──┼──┼───┤
│  │   │   │   /cache    ────────────────────────────────────────────────► │──┼──┼───┤
│  │   │   └───────────────────────────────────────────────────────────────┘  │  │   │
│  │   └──────────────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────────────┘   │
│                                        │                                           │
└────────────────────────────────────────│───────────────────────────────────────────┘
                                         │
                                         ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                      AZURE PERSISTENT STORAGE (1TB Azure Disk)                       │
│                                                                                     │
│   ┌─────────────────────────────────────────────────────────────────────────────┐   │
│   │                         /mnt/usf-storage (1TB)                               │   │
│   │                                                                              │   │
│   │   ├── /data/                          # Training data & outputs             │   │
│   │   │   ├── /uploads/                   # User uploaded datasets              │   │
│   │   │   ├── /datasets/                  # Registered datasets                 │   │
│   │   │   ├── /output/                    # Training outputs (LoRA adapters)    │   │
│   │   │   ├── /checkpoints/               # Training checkpoints                │   │
│   │   │   └── /logs/                      # Training logs                       │   │
│   │   │                                                                          │   │
│   │   ├── /models/                        # Pre-downloaded base models          │   │
│   │   │   ├── /Qwen--Qwen2.5-7B-Instruct/ # Example model                       │   │
│   │   │   ├── /meta-llama--Llama-3.1-8B/  # Example model                       │   │
│   │   │   └── ...                                                               │   │
│   │   │                                                                          │   │
│   │   └── /cache/                         # HuggingFace cache                   │   │
│   │       ├── /hub/                       # Downloaded model files              │   │
│   │       └── /datasets/                  # Cached datasets                     │   │
│   └─────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

## Storage Path Configuration

### Current Implementation Storage Paths

| Purpose | Environment Variable | Default Path | Mounted Path (AKS) |
|---------|---------------------|--------------|-------------------|
| Training Outputs | `OUTPUT_PATH` | `/app/data/output` | `/mnt/usf-storage/data/output` |
| Checkpoints | `CHECKPOINT_PATH` | `/app/data/checkpoints` | `/mnt/usf-storage/data/checkpoints` |
| Training Logs | `LOG_PATH` | `/app/data/logs` | `/mnt/usf-storage/data/logs` |
| Dataset Uploads | Internal | `/app/data/uploads` | `/mnt/usf-storage/data/uploads` |
| Base Models | `USF_MODEL_PATH` | `/models` | `/mnt/usf-storage/models` |
| HuggingFace Cache | `HF_HOME` | `/root/.cache/huggingface` | `/mnt/usf-storage/cache` |

### What Gets Stored Where

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           STORAGE ALLOCATION (~1TB)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   /mnt/usf-storage/                                                         │
│   │                                                                         │
│   ├── /models/ (~500GB)                   ◄── PRE-DOWNLOADED BASE MODELS   │
│   │   │                                                                     │
│   │   │   Purpose: Store base models offline for air-gapped training       │
│   │   │   Contents:                                                         │
│   │   │     - Model weights (.safetensors, .bin)                           │
│   │   │     - Tokenizer files                                              │
│   │   │     - Config files                                                 │
│   │   │                                                                     │
│   │   └── Qwen--Qwen2.5-7B-Instruct/     (~15GB per 7B model)             │
│   │       ├── config.json                                                  │
│   │       ├── tokenizer.json                                               │
│   │       ├── model-00001-of-00004.safetensors                            │
│   │       ├── model-00002-of-00004.safetensors                            │
│   │       ├── model-00003-of-00004.safetensors                            │
│   │       └── model-00004-of-00004.safetensors                            │
│   │                                                                         │
│   ├── /data/ (~300GB)                     ◄── TRAINING DATA & OUTPUTS      │
│   │   │                                                                     │
│   │   ├── /uploads/                       User uploaded datasets            │
│   │   │   └── my_dataset.jsonl            (JSONL, JSON, CSV)               │
│   │   │                                                                     │
│   │   ├── /output/                        Trained LoRA adapters            │
│   │   │   └── {job_id}/                                                    │
│   │   │       ├── adapter_config.json     (~10-100MB per LoRA)            │
│   │   │       ├── adapter_model.safetensors                               │
│   │   │       └── training_log.json                                       │
│   │   │                                                                     │
│   │   ├── /checkpoints/                   Training checkpoints             │
│   │   │   └── {job_id}/                                                    │
│   │   │       └── checkpoint-{step}/      (~2-5GB per checkpoint)         │
│   │   │           ├── optimizer.pt                                        │
│   │   │           ├── scheduler.pt                                        │
│   │   │           └── pytorch_model.bin                                   │
│   │   │                                                                     │
│   │   └── /logs/                          Training logs                    │
│   │       └── {job_id}.log                                                │
│   │                                                                         │
│   └── /cache/ (~200GB)                    ◄── HUGGINGFACE CACHE            │
│       │                                                                     │
│       │   Purpose: Cache for on-demand model downloads                      │
│       │   (Used when SUPPORTED_MODEL_SOURCES includes 'huggingface')        │
│       │                                                                     │
│       ├── /hub/                           Model cache                      │
│       │   └── models--Qwen--Qwen2.5-7B-Instruct/                          │
│       │       └── snapshots/                                               │
│       │           └── {commit_hash}/                                       │
│       │                                                                     │
│       └── /datasets/                      Dataset cache                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Complete Compute Flow

### 1. Installation & Deployment Flow

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         DEPLOYMENT FLOW                                       │
└──────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────┐
  │  1. Build Docker    │
  │     Image           │
  │  (GitHub Actions)   │
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │  2. Push to Docker  │
  │     Hub / ACR       │──────────────────────┐
  │                     │                      │
  └──────────┬──────────┘                      │
             │                                 │
             ▼                                 │
  ┌─────────────────────┐                      │
  │  3. Create AKS      │                      │
  │     Resources       │                      │
  │  - StorageClass     │                      │
  │  - PVC (1TB)        │                      │
  │  - Deployment       │                      │
  │  - Service          │                      │
  │  - Ingress          │                      │
  └──────────┬──────────┘                      │
             │                                 │
             ▼                                 │
  ┌─────────────────────┐                      │
  │  4. AKS Pulls       │◄─────────────────────┘
  │     Docker Image    │
  │                     │
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │  5. Pod Starts      │
  │  - Mounts PVC       │
  │  - GPU attached     │
  │  - Backend starts   │
  │  - Frontend starts  │
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │  6. System Ready    │
  │  - Port 3000 exposed│
  │  - via LoadBalancer │
  │    or Ingress       │
  └─────────────────────┘
```

### 2. Model Loading Flow

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                          MODEL LOADING FLOW                                   │
└──────────────────────────────────────────────────────────────────────────────┘

  User selects model in UI
           │
           ▼
  ┌─────────────────────┐
  │  Check Model Source │
  └──────────┬──────────┘
             │
     ┌───────┼───────┐
     │       │       │
     ▼       ▼       ▼
  ┌─────┐ ┌─────┐ ┌───────────┐
  │Local│ │ HF  │ │ModelScope │
  └──┬──┘ └──┬──┘ └─────┬─────┘
     │       │          │
     ▼       ▼          ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    LOCAL PATH RESOLUTION                         │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                  │
  │  Option A: Pre-downloaded (Recommended for Air-Gapped)          │
  │  ─────────────────────────────────────────────────────          │
  │  Path: /mnt/usf-storage/models/{org}--{model}/                  │
  │  Example: /mnt/usf-storage/models/Qwen--Qwen2.5-7B-Instruct/   │
  │                                                                  │
  │  Option B: HuggingFace Cache (On-Demand Download)               │
  │  ─────────────────────────────────────────────────              │
  │  Cache: /mnt/usf-storage/cache/hub/models--{org}--{model}/     │
  │  First run: Downloads to cache (~15GB for 7B model)            │
  │  Subsequent: Uses cached version                                │
  │                                                                  │
  │  Option C: Direct Local Path                                    │
  │  ───────────────────────────                                    │
  │  User specifies: /mnt/usf-storage/models/custom-model/         │
  │                                                                  │
  └─────────────────────────────────────────────────────────────────┘
           │
           ▼
  ┌─────────────────────┐
  │  Validate Model     │
  │  - Check config.json│
  │  - Check weights    │
  │  - Check tokenizer  │
  └──────────┬──────────┘
             │
             ▼
  ┌─────────────────────┐
  │  Register in Model  │
  │  Registry (SQLite)  │
  └─────────────────────┘
```

### 3. Training Execution Flow

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         TRAINING EXECUTION FLOW                               │
└──────────────────────────────────────────────────────────────────────────────┘

  User clicks "Start Training"
           │
           ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    JOB CREATION                                  │
  ├─────────────────────────────────────────────────────────────────┤
  │  1. Create TrainingJob record in SQLite DB                      │
  │  2. Generate unique job_id                                      │
  │  3. Create directories:                                         │
  │     - /mnt/usf-storage/data/output/{job_id}/                   │
  │     - /mnt/usf-storage/data/checkpoints/{job_id}/              │
  │  4. Lock config (no modifications after start)                  │
  └──────────────────────────────┬──────────────────────────────────┘
                                 │
                                 ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    GPU ALLOCATION                                │
  ├─────────────────────────────────────────────────────────────────┤
  │  1. Check GPU availability (NVIDIA SMI)                         │
  │  2. Clear GPU memory (gc.collect + torch.cuda.empty_cache)     │
  │  3. Set CUDA_VISIBLE_DEVICES                                    │
  └──────────────────────────────┬──────────────────────────────────┘
                                 │
                                 ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    MODEL LOADING                                 │
  ├─────────────────────────────────────────────────────────────────┤
  │  1. Load base model from /models or cache                       │
  │  2. Apply quantization if configured (4-bit, 8-bit)            │
  │  3. Initialize LoRA adapters                                    │
  │  4. Move to GPU                                                 │
  │                                                                  │
  │  Memory: ~16GB VRAM for 7B model with 4-bit quantization       │
  └──────────────────────────────┬──────────────────────────────────┘
                                 │
                                 ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    DATASET LOADING                               │
  ├─────────────────────────────────────────────────────────────────┤
  │  Source: /mnt/usf-storage/data/uploads/{dataset_file}          │
  │  Formats: JSONL, JSON, CSV                                      │
  │  Processing:                                                     │
  │    1. Load dataset                                              │
  │    2. Apply chat template                                       │
  │    3. Tokenize                                                  │
  │    4. Create DataLoader                                         │
  └──────────────────────────────┬──────────────────────────────────┘
                                 │
                                 ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    TRAINING LOOP                                 │
  ├─────────────────────────────────────────────────────────────────┤
  │  For each epoch:                                                 │
  │    For each batch:                                              │
  │      1. Forward pass                                            │
  │      2. Compute loss                                            │
  │      3. Backward pass                                           │
  │      4. Optimizer step                                          │
  │      5. Log metrics → WebSocket → Frontend                      │
  │                                                                  │
  │  Periodic Actions:                                              │
  │    - Save checkpoint every N steps                              │
  │      → /mnt/usf-storage/data/checkpoints/{job_id}/             │
  │    - Log to /mnt/usf-storage/data/logs/{job_id}.log            │
  └──────────────────────────────┬──────────────────────────────────┘
                                 │
                                 ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │                    TRAINING COMPLETION                           │
  ├─────────────────────────────────────────────────────────────────┤
  │  1. Save final LoRA adapter:                                    │
  │     → /mnt/usf-storage/data/output/{job_id}/                   │
  │       ├── adapter_config.json                                   │
  │       ├── adapter_model.safetensors                             │
  │       └── training_args.json                                    │
  │                                                                  │
  │  2. Clear GPU memory                                            │
  │  3. Update job status → COMPLETED                               │
  │  4. Cleanup intermediate checkpoints (optional)                 │
  └─────────────────────────────────────────────────────────────────┘
```

### 4. Accessing Trained Models

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                       ACCESSING TRAINED MODELS                                │
└──────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │                    OUTPUT LOCATION                               │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                  │
  │  Trained LoRA Adapter:                                          │
  │  /mnt/usf-storage/data/output/{job_id}/                        │
  │                                                                  │
  │  Contents:                                                       │
  │  ├── adapter_config.json      (LoRA configuration)             │
  │  ├── adapter_model.safetensors (Trained weights, ~10-100MB)    │
  │  └── training_args.json       (Training parameters)            │
  │                                                                  │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │                    ACCESS METHODS                                │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                  │
  │  Method 1: kubectl cp (Download to local)                       │
  │  ─────────────────────────────────────────                      │
  │  kubectl cp usf-bios-pod:/mnt/usf-storage/data/output/{job_id} │
  │             ./my-lora-adapter/                                   │
  │                                                                  │
  │  Method 2: Azure File Share (Mount elsewhere)                   │
  │  ─────────────────────────────────────────────                  │
  │  Mount same PVC on inference pod                                │
  │  Path: /mnt/usf-storage/data/output/{job_id}                   │
  │                                                                  │
  │  Method 3: AzCopy (Backup to Blob Storage)                      │
  │  ─────────────────────────────────────────────                  │
  │  azcopy cp "/mnt/usf-storage/data/output/{job_id}"             │
  │             "https://account.blob.core.windows.net/outputs/"    │
  │                                                                  │
  │  Method 4: Download via UI (if implemented)                     │
  │  ─────────────────────────────────────────────                  │
  │  GET /api/jobs/{job_id}/download                                │
  │                                                                  │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │                    USING TRAINED ADAPTER                         │
  ├─────────────────────────────────────────────────────────────────┤
  │                                                                  │
  │  # In your inference code:                                      │
  │  from peft import PeftModel, PeftConfig                         │
  │  from transformers import AutoModelForCausalLM                  │
  │                                                                  │
  │  # Load base model                                              │
  │  base_model = AutoModelForCausalLM.from_pretrained(             │
  │      "/mnt/usf-storage/models/Qwen--Qwen2.5-7B-Instruct"       │
  │  )                                                              │
  │                                                                  │
  │  # Load LoRA adapter                                            │
  │  model = PeftModel.from_pretrained(                             │
  │      base_model,                                                │
  │      "/mnt/usf-storage/data/output/{job_id}"                   │
  │  )                                                              │
  │                                                                  │
  └─────────────────────────────────────────────────────────────────┘
```

## Region Configuration

### Azure Region Selection

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         REGION CONSIDERATIONS                                 │
└──────────────────────────────────────────────────────────────────────────────┘

  GPU Availability by Region (Example):
  
  ┌─────────────────────────────────────────────────────────────────┐
  │  Region              │ GPU SKU              │ Availability      │
  ├─────────────────────────────────────────────────────────────────┤
  │  eastus              │ Standard_NC6s_v3     │ ✅ Good          │
  │  eastus2             │ Standard_NC6s_v3     │ ✅ Good          │
  │  westus2             │ Standard_NC6s_v3     │ ⚠️ Limited       │
  │  westeurope          │ Standard_NC6s_v3     │ ✅ Good          │
  │  southeastasia       │ Standard_NC6s_v3     │ ⚠️ Limited       │
  │  uaenorth            │ Standard_NC6s_v3     │ ✅ Good          │
  └─────────────────────────────────────────────────────────────────┘

  Storage Considerations:
  
  ┌─────────────────────────────────────────────────────────────────┐
  │  Storage Type        │ Use Case             │ Performance       │
  ├─────────────────────────────────────────────────────────────────┤
  │  Azure Disk (SSD)    │ Best for training    │ ⚡ Fast IOPS     │
  │  Azure Files (SMB)   │ Multi-pod access     │ 🐌 Slower        │
  │  Azure NetApp Files  │ High-performance NFS │ ⚡⚡ Fastest     │
  └─────────────────────────────────────────────────────────────────┘

  Recommendation for Training:
  - Use Azure Premium SSD (managed-premium StorageClass)
  - 1TB provides ~5000 IOPS baseline
  - Sufficient for model loading and checkpoint saves
```

## Environment Variables for AKS

```yaml
# ConfigMap for USF BIOS
apiVersion: v1
kind: ConfigMap
metadata:
  name: usf-bios-config
data:
  # Storage paths (mounted from PVC)
  OUTPUT_PATH: "/mnt/usf-storage/data/output"
  CHECKPOINT_PATH: "/mnt/usf-storage/data/checkpoints"
  LOG_PATH: "/mnt/usf-storage/data/logs"
  
  # HuggingFace cache location
  HF_HOME: "/mnt/usf-storage/cache"
  TRANSFORMERS_CACHE: "/mnt/usf-storage/cache/hub"
  
  # Model source restrictions (optional)
  SUPPORTED_MODEL_SOURCES: "huggingface,modelscope,local"
  
  # GPU settings
  CUDA_VISIBLE_DEVICES: "0"
```

## Summary

| Component | Location | Size Estimate | Access Method |
|-----------|----------|---------------|---------------|
| **Base Models** | `/mnt/usf-storage/models/` | 15-50GB each | Mount or Pre-download |
| **Uploaded Datasets** | `/mnt/usf-storage/data/uploads/` | 1MB - 10GB | Upload via UI |
| **Training Outputs** | `/mnt/usf-storage/data/output/{job_id}/` | 10-100MB each | kubectl cp, API |
| **Checkpoints** | `/mnt/usf-storage/data/checkpoints/{job_id}/` | 2-5GB each | Internal use |
| **Logs** | `/mnt/usf-storage/data/logs/` | KB-MB each | kubectl logs |
| **HF Cache** | `/mnt/usf-storage/cache/` | 15-50GB per model | Auto-managed |
