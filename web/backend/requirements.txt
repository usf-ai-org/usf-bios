fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiofiles>=23.0.0

# HTTP client for async requests (vLLM server testing)
httpx>=0.25.0

# System metrics
nvidia-ml-py>=12.0.0
psutil>=5.9.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Security & encryption
cryptography>=41.0.0

# ML/Training dependencies
torch>=2.1.0
transformers>=4.33,<4.58
datasets>=3.0,<4.0
peft>=0.11,<0.19
trl>=0.15,<0.25
accelerate>=0.25.0
bitsandbytes>=0.41.0
sentencepiece>=0.1.99

# NOTE: If using custom usf_bios package, install it separately:
#   cd /path/to/usf-bios && pip install -e .
